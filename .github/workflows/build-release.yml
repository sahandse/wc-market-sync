name: Build Plugin ZIP

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare plugin
        run: |
          mkdir -p build/wc-market-sync
          rsync -av --exclude='.git' --exclude='.github' --exclude='build' ./ build/wc-market-sync/

      - name: Create ZIP
        run: |
          cd build
          zip -r wc-market-sync.zip wc-market-sync

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: wc-market-sync
          path: build/wc-market-sync.zip

      - name: Publish GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: build/wc-market-sync.zip
          generate_release_notes: true
